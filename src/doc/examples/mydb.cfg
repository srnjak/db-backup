# MySQL/MariaDB credentials
DB_HOST=localhost
DB_PORT=3306
DB_USER=backup
DB_PASSWORD=dbpass

# Backup destination directory
BACKUP_DIR=/backup/db/mydb

# Prefix for backup subdirectory
BACKUP_SUBDIR_PREFIX=mydb

KEEP_BACKUPS_DAYS=10

# List of databases to backup
DB_NAMES=(
        "database_one" 
        "database_two"
)
